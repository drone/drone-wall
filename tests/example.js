"use strict";

/* Tests */

var ptor = protractor.getInstance();

describe( "The page", function ()
{
	it( "should do something", function ()
	{
		browser.get( "http://the/site/path" );

		// Some testin why not
		
	} );

} );
