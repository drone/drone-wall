// Karma runs unit tests that do not interact with the UI
// put your *.spec.js files next to your actual filters, controllers and such, not in this /tests folder

"use strict";

describe( "Subject", function ()
{
    it( "should have a behavior", function ()
    {
        expect( true ).toBeDefined();
    } );
} );
